# ── Core runtime (always installed) ──────────────────────────────────────────
moviepy==2.1.2
edge_tts==6.1.19
fastapi==0.115.6
uvicorn==0.32.1
openai==1.56.1
loguru==0.7.3
google-generativeai==0.8.3
python-multipart==0.0.19
pydub>=0.25.1
gTTS>=2.4.0
pyyaml
requests>=2.31.0
toml>=0.10.2

# ── Optional / provider-specific (lazy-imported at runtime) ───────────────────
# Uncomment locally if you need these providers:
# g4f==0.5.2.2                       # GPT4Free — huge (pulls torch)
# faster-whisper==1.1.0              # Whisper subtitles — pulls torch+ctranslate2
# azure-cognitiveservices-speech==1.41.1  # Azure TTS binary SDK — ~250MB
# dashscope==1.20.14                 # Alibaba DashScope LLM
# redis==5.2.0                       # Redis cache (not required for basic usage)
